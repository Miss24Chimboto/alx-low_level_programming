#include <stdio.h>
/**
 * main - a program that prints its name, followed by a new line
 * @argc: counts arguments, unused in this program
 * @argv: argument array
 *
 * Return: 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{
printf("%s\n", argv[0]);

return (0);
}
