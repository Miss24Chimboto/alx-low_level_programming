#include <stdio.h>
/**
 * main - enables program to work
 *
 * Return: enables it to run successfully
 */
int main(void)
{

	char c;

	for (c = 'a'; c <= 'z'; c++)
	putchar(c);
	putchar('\n');

return (0);
}

