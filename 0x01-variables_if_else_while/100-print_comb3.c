#include <stdio.h>
/**
* main - enables it to run successfully
* Description: Printing different combinations of two digits
* Return: 0
*/
int main(void)
{
	int a, b;

	a = 48;
	b = 48;

while (b < 58)
	{
a = 48;
while (a < 58)
{
if (b != i && b < i)
{
putchar(b);
putchar(a);
if (a == 57 && b == 56)
{
break;
}
putchar(',');
putchar(' ');
}
i++;
}
e++;
}
putchar('\n');
return (0);
}
